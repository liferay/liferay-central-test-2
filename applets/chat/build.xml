<?xml version="1.0"?>

<project name="chat" basedir="." default="compile">
	<property name="project.dir" value="../.." />

	<import file="${project.dir}/build-common-java.xml" />

	<property name="deploy.dir" value="${project.dir}/portal-web/docroot/applets/chat" />
	<property name="jar.file" value="${ant.project.name}" />

	<target name="deploy" depends="build-common-java.deploy">
		<copy file="${project.dir}/lib/development/nfc-client.jar" todir="${deploy.dir}" />
	</target>

	<target name="run" depends="compile">
		<java
			classname="com.liferay.applets.chat.LiferayChat"
			classpathref="project.classpath"
			fork="true"
		/>
	</target>
</project>