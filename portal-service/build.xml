<?xml version="1.0"?>

<project name="portal-service" basedir="." default="compile">
	<import file="../build-common-java.xml" />

	<property name="deploy.dir" value="${app.server.lib.global.dir}" />
	<property name="jar.file" value="${ant.project.name}" />

	<target name="compile">
		<mkdir dir="classes" />

		<copy todir="classes">
			<fileset dir="src" excludes="**/*.java" />
		</copy>

		<path id="web-lib.classpath">
			<fileset dir="${project.dir}/lib/development" includes="junit.jar" />
		</path>

		<javac
			classpathref="web.classpath"
			compiler="${javac.compiler}"
			debug="${javac.debug}"
			deprecation="${javac.deprecation}"
			destdir="classes"
			encoding="${javac.encoding}"
			includeAntRuntime="false"
			nowarn="${javac.nowarn}"
			srcdir="src"
		/>
	</target>

	<target name="jar" depends="compile">
		<antcall target="manifest" />

		<manifest file="classes/META-INF/MANIFEST.MF" mode="update">
			<attribute name="Bundle-Name" value="Liferay Portal Service" />
			<attribute name="Bundle-SymbolicName" value="com.liferay.portal.service;singleton:=true" />
		</manifest>

		<jar
			basedir="classes"
			jarfile="${jar.file}.jar"
			manifest="classes/META-INF/MANIFEST.MF"
		/>
	</target>
</project>