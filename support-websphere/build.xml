<?xml version="1.0"?>

<project name="support-websphere" basedir="." default="compile" xmlns:antelope="antlib:ise.antelope.tasks">
	<import file="../build-common-java.xml" />

	<property name="deploy.dir" value="${app.server.lib.global.dir}" />
	<property name="jar.file" value="${ant.project.name}" />
	<property name="release.info.version" value="1.0.0" />

	<target name="clean">
		<antcall target="build-common-java.clean" />

		<delete file="${deploy.dir}/${jar.file}.jar" />
	</target>

	<target name="compile">
		<mkdir dir="classes" />

		<path id="web-lib.classpath">
			<fileset dir="${app.server.websphere.dir}/plugins" includes="com.ibm.ws.runtime.jar" />
		</path>

		<record action="start" name="${ant.project.name}/javac.output.txt" />

		<javac
			classpathref="web.classpath"
			compiler="${javac.compiler}"
			debug="${javac.debug}"
			deprecation="${javac.deprecation}"
			destdir="classes"
			encoding="${javac.encoding}"
			includeAntRuntime="false"
			nowarn="${javac.nowarn}"
			srcdir="src"
		/>

		<record action="stop" name="${ant.project.name}/javac.output.txt" />

		<loadfile srcfile="javac.output.txt" property="javac.output" />

		<if>
			<or>
				<contains string="${javac.output}" substring="a deprecated API" />
				<contains string="${javac.output}" substring="[deprecation]" />
			</or>
			<then>
				<echo append="true" file="../javac.output.txt" message="${line.separator}${ant.project.name}${line.separator}" />
				<echo append="true" file="../javac.output.txt" message="${javac.output}" />
			</then>
		</if>

		<delete file="javac.output.txt" />
	</target>

	<target name="deploy" depends="jar">
		<delete file="${deploy.dir}/${ant.project.name}.jar" />

		<antcall target="build-common-java.deploy" />
	</target>
</project>