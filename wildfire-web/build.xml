<?xml version="1.0"?>

<project name="wildfire-web" basedir="." default="war">
	<import file="../build-common-web.xml" />

	<property name="wildfire.file" value="wildfire-3.0.1.war" />
	<property name="war.file" value="${ant.project.name}" />

	<target name="clean" depends="build-common-web.clean">
		<delete dir="tmp" />
	</target>

	<target name="merge">
		<if>
			<not>
				<uptodate srcfile="${wildfire.file}" targetfile="tmp" />
			</not>
			<then>
				<delete dir="tmp" />
				<mkdir dir="tmp" />

				<unjar dest="tmp" src="${wildfire.file}" />

				<copy todir="tmp" overwrite="true">
					<fileset dir="docroot" />
				</copy>
			</then>
		</if>

		<copy todir="tmp" overwrite="false">
			<fileset dir="docroot" />
		</copy>
	</target>

	<target name="war" depends="merge">
		<war
			basedir="tmp"
			destfile="${war.file}.war"
			excludes="WEB-INF/web.xml"
			webxml="docroot/WEB-INF/web.xml"
		/>
	</target>

	<target name="deploy" depends="build-common-web.deploy">
		<copy todir="${app.server.dir}/wildfireHome">
			<fileset dir="wildfireHome" />
		</copy>
	</target>
</project>