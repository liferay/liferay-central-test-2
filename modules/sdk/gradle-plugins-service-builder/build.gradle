dependencies {
	compile gradleApi()
	compile group: "com.liferay", name: "com.liferay.gradle.util", version: "1.0.23"

	provided project(":util:portal-tools-service-builder")
}

updateFileVersions {
	match(/"com\.liferay\.gradle\.plugins\.service\.builder", version: "(.+)"/, "../gradle-plugins/build.gradle")
}