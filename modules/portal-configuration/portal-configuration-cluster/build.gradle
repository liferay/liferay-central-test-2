dependencies {
	compile group: "org.osgi", name: "org.osgi.core", version: "6.0.0"
	compile project(":portal-configuration:portal-configuration-persistence")
}

liferay {
	deployDir = file("${liferayHome}/osgi/portal")
}