dependencies {
	provided group: "org.osgi", name: "org.osgi.core", version: "6.0.0"
	provided project(":apps:static:portal-target-platform-indexer:portal-target-platform-indexer")
}

liferay {
	deployDir = file("${liferayHome}/tools/${projectDir.name}")
}