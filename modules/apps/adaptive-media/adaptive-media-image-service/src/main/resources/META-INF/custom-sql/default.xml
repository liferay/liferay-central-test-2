<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.adaptive.media.image.service.persistence.AdaptiveMediaImageFinder.countByDLLiferayFileEntry">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				DLFileEntry
			WHERE
				(DLFileEntry.companyId = ?) AND
				(DLFileEntry.groupId = DLFileEntry.repositoryId)
		]]>
	</sql>
	<sql id="com.liferay.adaptive.media.image.service.persistence.AdaptiveMediaImageFinder.countByBlogsFileEntry">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				DLFileEntry
			WHERE
				(DLFileEntry.companyId = ?) AND
				(DLFileEntry.classNameId = ?)
		]]>
	</sql>
</custom-sql>