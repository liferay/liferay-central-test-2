dependencies {
	compile group: "com.liferay.portal", name: "portal-service", version: liferay.portalVersion
	compile project(":apps:product-navigation:product-navigation-control-menu-web")
}

liferay {
	deployDir = file("${liferayHome}/osgi/modules")
}