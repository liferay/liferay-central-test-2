var $ = AUI.$;var _ = AUI._;

Liferay.SPA = Liferay.SPA || {};
Liferay.SPA.cacheExpirationTime = #cacheExpirationTime#;
Liferay.SPA.clearScreensCache = #clearScreensCache#;
Liferay.SPA.debugEnabled = #debugEnabled#;
Liferay.SPA.excludedPaths = #excludedPaths#;
Liferay.SPA.loginRedirect = '#loginRedirect#';
Liferay.SPA.navigationExceptionSelectors = '#navigationExceptionSelectors#';
Liferay.SPA.requestTimeout = #requestTimeout#;
Liferay.SPA.userNotification = {
	message: '#message#',
	timeout: #timeout#,
	title: '#title#'
};

frontendJsSpaWebLiferayInitEs.default.init(
	function(app) {
		app.setPortletsBlacklist(#portletsBlacklist#);
		app.setValidStatusCodes(#validStatusCodes#);
	}
);
