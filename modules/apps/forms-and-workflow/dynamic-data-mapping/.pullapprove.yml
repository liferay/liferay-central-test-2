version: 2
groups:
  code-review:
    approve_by_comment:
      approve_regex: '^(Approved|:\+1:|LGTM)'
      enabled: true
      reject_regex: '^(Rejected|:-1:)'
    required: 1
    reset_on_push:
      enabled: false
    users:
    - leoadb