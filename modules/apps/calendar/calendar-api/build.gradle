dependencies {
	compile group: "com.liferay", name: "com.google.ical", version: "20110304"
	compile group: "org.osgi", name: "org.osgi.core", version: "5.0.0"
}

liferay {
	deployDir = file("${liferayHome}/osgi/modules")
}