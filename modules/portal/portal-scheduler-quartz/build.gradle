dependencies {
	compile project(":portal:portal-scheduler")

	provided group: "org.quartz-scheduler", name: "quartz", transitive: false, version: "2.1.7"
}

liferay {
	deployDir = file("${liferayHome}/osgi/portal")
}