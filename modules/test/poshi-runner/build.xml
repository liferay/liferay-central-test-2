<?xml version="1.0"?>
<!DOCTYPE project>

<project>
	<import file="../../../tools/sdk/build-common-osgi-plugin.xml" />

	<!--<property name="auto.deploy.dir" value="${liferay.home}/osgi/modules" />-->

	<target name="junit-run" depends="compile">
		<mkdir dir="test-results" />

		<pathconvert property="classpath" refid="plugin.classpath" />

		<junit printsummary="on" showoutput="true">
			<test name="com.liferay.poshi.runner.PoshiRunner" outfile="test-results" />
			<classpath>
				<pathelement path="${classpath}" />
				<pathelement location="src" />
			</classpath>
			<formatter type="xml" />
		</junit>
	</target>

	<target name="start-poshi-runner">
		<antcall target="junit-run" />
	</target>
</project>