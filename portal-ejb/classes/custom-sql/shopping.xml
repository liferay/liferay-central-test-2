<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.shopping.service.persistence.ShoppingItemFinder.countByCategoryIds">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				ShoppingItem
			WHERE
				[$CATEGORY_ID$]
		]]>
	</sql>
	<sql id="com.liferay.portlet.shopping.service.persistence.ShoppingCouponFinder.countByC_G_C_A_DT">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				ShoppingCoupon
			WHERE
				(groupId = ?) AND
				(companyId = ?) AND
				(
					(couponId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(active_ = ?) [$AND_OR_CONNECTOR$]
					(discountType = ? [$AND_OR_NULL_CHECK$])
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.shopping.service.persistence.ShoppingOrderFinder.countByO_G_C_U_PPPS">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				ShoppingOrder
			WHERE
				(groupId = ?) AND
				(companyId = ?) AND
				(
					(orderId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(userId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingFirstName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingLastName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingEmailAddress LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingFirstName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingLastName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingEmailAddress LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(ppPaymentStatus = ?)
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.shopping.service.persistence.ShoppingCouponFinder.findByC_G_C_A_DT">
		<![CDATA[
			SELECT
				{ShoppingCoupon.*}
			FROM
				ShoppingCoupon
			WHERE
				(groupId = ?) AND
				(companyId = ?) AND
				(
					(couponId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(active_ = ?) [$AND_OR_CONNECTOR$]
					(discountType = ? [$AND_OR_NULL_CHECK$])
				)
			ORDER BY
				createDate ASC
		]]>
	</sql>	
	<sql id="com.liferay.portlet.shopping.service.persistence.ShoppingOrderFinder.findByO_G_C_U_PPPS">
		<![CDATA[
			SELECT
				{ShoppingOrder.*}
			FROM
				ShoppingOrder
			WHERE
				(groupId = ?) AND
				(companyId = ?) AND
				(
					(orderId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(userId LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingFirstName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingLastName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(billingEmailAddress LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingFirstName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingLastName LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(shippingEmailAddress LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(ppPaymentStatus = ?)
				)
			ORDER BY
				createDate ASC
		]]>
	</sql>
</custom-sql>