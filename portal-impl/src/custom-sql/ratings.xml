<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.ratings.service.persistence.RatingsEntryFinder.findByU_C_CS">
		<![CDATA[
			SELECT
				{RatingsEntry.*}
			FROM
				RatingsEntry
			WHERE
				(RatingsEntry.userId = ?) AND
				(RatingsEntry.classNameId = ?) AND
				(RatingsEntry.classPK IN ([$CLASSPKS$]))
		]]>
	</sql>
	<sql id="com.liferay.portlet.ratings.service.persistence.RatingsStatsFinder.findByC_CS">
		<![CDATA[
			SELECT
				{RatingsStats.*}
			FROM
				RatingsStats
			WHERE
				(RatingsStats.classNameId = ?) AND
				(RatingsStats.classPK IN ([$CLASSPKS$]))
		]]>
	</sql>
</custom-sql>