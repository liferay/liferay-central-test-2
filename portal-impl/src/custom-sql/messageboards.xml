<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.countByS_G_U_P">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBCategory.categoryId) AS COUNT_VALUE
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBCategory.parentCategoryId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.findByS_G_U_P">
		<![CDATA[
			SELECT
				DISTINCT {MBCategory.*}
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(MBCategory.parentCategoryId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBCategory.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByC_T">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				(createDate <= ?) AND
				(threadId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_C_U_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT A.threadId) AS COUNT_VALUE
			FROM
				MBMessage A
			INNER JOIN
				MBMessage B ON
					(B.messageId = A.rootMessageId)
			WHERE
				(A.groupId = ?) AND
				(A.categoryId = ?) AND
				(A.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_C_U_A_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT A.threadId) AS COUNT_VALUE
			FROM
				MBMessage A
			INNER JOIN
				MBMessage B ON
					(B.messageId = A.rootMessageId)
			WHERE
				(A.groupId = ?) AND
				(A.categoryId = ?) AND
				(A.userId = ?) AND
				(A.anonymous = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByNoAssets">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			WHERE
				(messageId NOT IN(
					SELECT
						classPK
					FROM
						AssetEntry
					WHERE
						classNameId = [$CLASS_NAME_ID_COM.LIFERAY.PORTLET.MESSAGEBOARDS.MODEL.MBMESSAGE$]
				))
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_C_U_S">
		<![CDATA[
			SELECT
				A.threadId
			FROM
				MBMessage A
			INNER JOIN
				MBMessage B ON
					(B.messageId = A.rootMessageId)
			WHERE
				(A.groupId = ?) AND
				(A.categoryId = ?) AND
				(A.userId = ?)
			GROUP BY
				A.threadId
			ORDER BY
				MAX(A.priority) DESC,
				MAX(A.modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_C_U_A_S">
		<![CDATA[
			SELECT
				A.threadId
			FROM
				MBMessage A
			INNER JOIN
				MBMessage B ON
					(B.messageId = A.rootMessageId)
			WHERE
				(A.groupId = ?) AND
				(A.categoryId = ?) AND
				(A.userId = ?) AND
				(A.anonymous = ?)
			GROUP BY
				A.threadId
			ORDER BY
				MAX(A.priority) DESC,
				MAX(A.modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByG_C">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByG_C_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByS_G_C_U_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByG_C">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByG_C_S">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByS_G_C_U_S">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				MBMessage ON
					(MBMessage.messageId = MBThread.rootMessageId)
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(MBThread.categoryId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
</custom-sql>