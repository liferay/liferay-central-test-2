<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.countByS_G_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBCategory.categoryId) AS COUNT_VALUE
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBCategoryFinder.findByS_G_U">
		<![CDATA[
			SELECT
				DISTINCT {MBCategory.*}
			FROM
				MBCategory
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBCategory.categoryId)
			WHERE
				(MBCategory.groupId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBCategory.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT threadId) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_U_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT threadId) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(status = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_U_A">
		<![CDATA[
			SELECT
				COUNT(DISTINCT threadId) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(anonymous = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.countByG_U_S_A">
		<![CDATA[
			SELECT
				COUNT(DISTINCT threadId) AS COUNT_VALUE
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(status = ?) AND
				(anonymous = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByNoAssets">
		<![CDATA[
			SELECT
				{MBMessage.*}
			FROM
				MBMessage
			WHERE
				(messageId NOT IN(
					SELECT
						classPK
					FROM
						AssetEntry
					WHERE
						classNameId = [$CLASS_NAME_ID_COM.LIFERAY.PORTLET.MESSAGEBOARDS.MODEL.MBMESSAGE$]
				))
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_U">
		<![CDATA[
			SELECT
				threadId
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?)
			GROUP BY
				threadId
			ORDER BY
				MAX(priority) DESC,
				MAX(modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_U_S">
		<![CDATA[
			SELECT
				threadId
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(status = ?)
			GROUP BY
				threadId
			ORDER BY
				MAX(priority) DESC,
				MAX(modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_U_S_A">
		<![CDATA[
			SELECT
				threadId
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(status = ?) AND
				(anonymous = ?)
			GROUP BY
				threadId
			ORDER BY
				MAX(priority) DESC,
				MAX(modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBMessageFinder.findByG_U_A">
		<![CDATA[
			SELECT
				threadId
			FROM
				MBMessage
			WHERE
				(groupId = ?) AND
				(userId = ?) AND
				(anonymous = ?)
			GROUP BY
				threadId
			ORDER BY
				MAX(priority) DESC,
				MAX(modifiedDate) DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByS_G_U">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(Subscription.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.countByS_G_U_S">
		<![CDATA[
			SELECT
				COUNT(DISTINCT MBThread.threadId) AS COUNT_VALUE
			FROM
				MBThread
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(Subscription.userId = ?) AND
				(MBThread.status = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByS_G_U">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(Subscription.userId = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
	<sql id="com.liferay.portlet.messageboards.service.persistence.MBThreadFinder.findByS_G_U_S">
		<![CDATA[
			SELECT
				DISTINCT {MBThread.*}
			FROM
				MBThread
			INNER JOIN
				Subscription ON
					(Subscription.classNameId = ?) AND
					(Subscription.classPK = MBThread.threadId)
			WHERE
				(MBThread.groupId = ?) AND
				(Subscription.userId = ?) AND
				(MBThread.status = ?)
			ORDER BY
				MBThread.priority DESC,
				MBThread.lastPostDate DESC
		]]>
	</sql>
</custom-sql>