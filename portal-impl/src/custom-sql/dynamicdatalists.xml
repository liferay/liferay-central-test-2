<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDLRecordSetFinder.countByC_G_R_N_D">
		<![CDATA[
			SELECT
				COUNT(DISTINCT recordSetId) AS COUNT_VALUE
			FROM
				DDLRecordSet
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(recordSetKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDLRecordSetFinder.findByC_G_R_N_D">
		<![CDATA[
			SELECT
				{DDLRecordSet.*}
			FROM
				DDLRecordSet
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(recordSetKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
			ORDER BY
				DDLRecordSet.recordSetId DESC
		]]>
	</sql>
</custom-sql>