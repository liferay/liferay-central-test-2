<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDMEntryFinder.countByC_G_E_N_D">
		<![CDATA[
			SELECT
				COUNT(DISTINCT listId) AS COUNT_VALUE
			FROM
				DDMEntry
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(entryKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDMEntryFinder.findByC_G_E_N_D">
		<![CDATA[
			SELECT
				{DDMEntry.*}
			FROM
				DDMEntry
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(entryKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
			ORDER BY
				DDMEntry.listId DESC
		]]>
	</sql>
</custom-sql>