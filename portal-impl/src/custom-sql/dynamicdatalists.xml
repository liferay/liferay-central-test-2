<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDLEntryFinder.countByC_G_E_N_D">
		<![CDATA[
			SELECT
				COUNT(DISTINCT entryId) AS COUNT_VALUE
			FROM
				DDLEntry
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(entryKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.dynamicdatalists.service.persistence.DDLEntryFinder.findByC_G_E_N_D">
		<![CDATA[
			SELECT
				{DDLEntry.*}
			FROM
				DDLEntry
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(entryKey LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(description LIKE ? [$AND_OR_NULL_CHECK$])
				)
			ORDER BY
				DDLEntry.entryId DESC
		]]>
	</sql>
</custom-sql>