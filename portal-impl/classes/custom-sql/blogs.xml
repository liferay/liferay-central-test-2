<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.blogs.service.persistence.BlogsEntryFinder.countByCategoryIds">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				BlogsEntry
			WHERE
				[$CATEGORY_ID$]
		]]>
	</sql>
	<sql id="com.liferay.portlet.blogs.service.persistence.BlogsEntryFinder.findByNoAssets">
		<![CDATA[
			SELECT
				{BlogsEntry.*}
			FROM
				BlogsEntry
			WHERE
				(entryId NOT IN(
					SELECT
						classPK
					FROM
						TagsAsset
					WHERE
						classNameId = [$CLASS_NAME_ID_COM.LIFERAY.PORTLET.BLOGS.MODEL.BLOGSENTRY$]
				))
		]]>
	</sql>
</custom-sql>