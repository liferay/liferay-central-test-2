<?xml version="1.0"?>

<custom-sql>
	<sql id="com.liferay.portlet.tasks.service.persistence.TasksProposalFinder.countByC_G_N_U">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				TasksProposal
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(userName) LIKE ? [$AND_OR_NULL_CHECK$])
				)
		]]>
	</sql>
	<sql id="com.liferay.portlet.tasks.service.persistence.TasksProposalFinder.findByC_G_N_U">
		<![CDATA[
			SELECT
				{TasksProposal.*}
			FROM
				TasksProposal
			WHERE
				(companyId = ?) AND
				(groupId = ?) AND
				(
					(lower(name) LIKE ? [$AND_OR_NULL_CHECK$]) [$AND_OR_CONNECTOR$]
					(lower(userName) LIKE ? [$AND_OR_NULL_CHECK$])
				)
			ORDER BY
				dueDate ASC,
				createDate ASC
		]]>
	</sql>
	<sql id="com.liferay.portlet.tasks.service.persistence.TasksProposalFinder.countByC_G_R">
		<![CDATA[
			SELECT
				COUNT(*) AS COUNT_VALUE
			FROM
				TasksProposal
			INNER JOIN
				TasksReview ON
					(TasksReview.proposalId = TasksProposal.proposalId)
			WHERE
				(TasksProposal.companyId = ?) AND
				(TasksProposal.groupId = ?) AND
				(TasksReview.userId = ?)
		]]>
	</sql>
	<sql id="com.liferay.portlet.tasks.service.persistence.TasksProposalFinder.findByC_G_R">
		<![CDATA[
			SELECT
				DISTINCT {TasksProposal.*}
			FROM
				TasksProposal
			INNER JOIN
				TasksReview ON
					(TasksReview.proposalId = TasksProposal.proposalId)
			WHERE
				(TasksProposal.companyId = ?) AND
				(TasksProposal.groupId = ?) AND
				(TasksReview.userId = ?)
			ORDER BY
				TasksProposal.dueDate ASC,
				TasksProposal.createDate ASC
		]]>
	</sql>
</custom-sql>