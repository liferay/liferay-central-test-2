evaluationDependsOn(':registry-api')

dependencies {
	compile fileTree(
		dir: liferayProjectDir + '/osgi/lib/common/core',
		include: '*.jar'
	)

	compile project(':registry-api')
}

project.ext.deployDir = project.hasProperty('appServerParentDir') ? project.appServerParentDir + "/osgi/core" : null